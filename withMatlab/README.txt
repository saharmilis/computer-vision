
---------------------------------------------------------
directions:

	run the script "run.m",
	and wait for 2-3 minutes.
	the output will be saved as file "newfile.avi"
---------------------------------------------------------



---------------------------------------------------------
In case of a new video:

	enter the project directory
	delete all images in directory "dir" 
	save your video as the video file in the project folder as "vid.mp4"
	run the script "fromVideoToImages.m"
	then run the script "run.m"
---------------------------------------------------------

